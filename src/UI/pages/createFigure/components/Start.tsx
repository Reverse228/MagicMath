import { observer } from "mobx-react-lite";
import React from "react";
import "../style/components_style/start_style/style.css";

const Start = () => {
  return <h2>Create 3 point...</h2>;
};

export default observer(Start);
